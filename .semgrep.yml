# Semgrep Configuration
# Local custom rules for this project. Public community rulesets
# (p/security-audit, p/owasp-top-ten, p/python) are pulled by the
# Security Gate workflow directly via `--config=p/...` so they don't
# need to be listed here.
#
# Documentation: https://semgrep.dev/docs/writing-rules/overview/

rules: []
